---
"@grancalavera/bridge": minor
---

Restructure project as a monorepo with npm workspaces. The library now lives in `packages/bridge/` and examples in `packages/examples/`. Added Claude Code and mise configuration.
